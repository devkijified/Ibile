export default function AdminDashboard() {
  return (
    <div style={{ padding: '24px' }}>
      <h1>Admin Dashboard</h1>
      <p>If you see this, the dashboard component is working.</p>
      <p>Check the console for data-fetching errors.</p>
    </div>
  );
}
